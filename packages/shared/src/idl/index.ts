/**
 * Placeholder for Anchor-generated IDL.
 *
 * After running `anchor build`, copy the generated IDL JSON file here
 * and create a typed client export.
 *
 * Usage:
 *   import { IDL } from "@repo/shared/idl";
 */

export const IDL_PLACEHOLDER = {
  version: "0.1.0",
  name: "open_xp",
  instructions: [],
  accounts: [],
  errors: [],
} as const;

export type OpenXpIDL = typeof IDL_PLACEHOLDER;
